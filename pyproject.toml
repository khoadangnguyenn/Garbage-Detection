[project]
name = "urax-garbage-pipeline"
version = "0.1.0"
description = "Recyclable-vs-not waste classification pipeline (URAxUTS Garbage Detection)"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["pipeline*"]
